const appbar = {
    backgroundColor: '#FFFFFF'
}

const appbarIcon = {
    color: "black"
}

const appbarTitleContainer = {
    flexGrow: 1,
    textAlign: 'center',
    fontSize: 22,
    color: 'black',
    marginLeft: -5
}

const appbarTitle = {
    margin: 0
}

const titleContainer = {
    width: 110
}

const title = {
    margin: '0px 5%',
    paddingTop: 50,
    maxWidth: 500
}

const primaryTitle = {
    color: '#3D4276', fontWeight: 'bold', fontSize: 12, margin: 0
}

const secondaryTitle = {
    color: '#949494', fontSize: 10, margin: 0
}

const button = {
    fontSize: 11,
    borderRadius: 10
}

const screenOneButtonIcon = {
    height: '30px'
}

const playButtonContainer = {
    backgroundColor: 'rgb(255, 114, 67)',
    width: 90,
    borderRadius: 12
}

const imageContainer = {
    top: -72,
    position: 'relative',
    right: -86,
    width: '100%'
}

const playButtonPrimary = {
    textAlign: 'center',
    margin: 8
}

const playButtonSecondary = {
    textAlign: 'center',
    fontWeight: 'bold',
    color: '#424242'
}

const screen1Action = {
    paddingTop: 10
}

const iconColor = {
    color: 'white'
}

const screen1Card = {
    padding: 10,
    borderRadius: 10,
    margin: '0px 5%',
    maxWidth: 500,
    backgroundColor: 'white'
}

const screen1Container = {
    height: '100vh',
    backgroundColor: '#CED2DC'
}

const actionContainer = {
    margin: 0
}
const closeButton = {
    marginLeft: 10,
    fontSize: 14,
    color: '#4B7BFF',
    backgroundColor: 'white',
    borderRadius: 10
}
const okButton = {
    marginRight: -15,
    fontSize: 14,
    color: 'white',
    backgroundColor: '#4B7BFF',
    borderRadius: 10
}

const gameTitle = {
    textAlign: 'center'
}
const gameBody = {
    margin: 0
}
const gameRadio = {
    borderRadius: 10,
    backgroundColor: '#EFF3FF'
}
const gameDivider = {
    width: '100%'
}

const screen2Container = {
    height: '100vh',
    backgroundColor: '#CED2DC',
    paddingTop: '20%'
}

const screen2Card = {
    padding: 10,
    borderRadius: 10,
    margin: '0px 5%',
    maxWidth: 500,
    backgroundColor: 'white'
}

const screen2Divider = {
    position: 'relative',
    top: '5px',
    marginBottom: 20
}

const screen2Button = {
    fontSize: 14,
    backgroundColor: '#4B7BFF',
    borderRadius: 10
}

const userContainer = {
    padding: 20,
    marginBottom: 15,
    borderRadius: 10
}

const userImageContainer = {
    padding: 0,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
}

const userImage = {
    borderRadius: '50%',
    padding: '5px 8px 5px 8px',
    height: '50px',
    width: '50px',
    bottom: '165px'
}

const userTextContainer = {
    padding: 0
}

const userInputTitle = {
    marginBottom: 0,
    color: 'grey',
    fontSize: 10
}

const userInput = {
    width: '100%',
    fontSize: 14,
    paddingBottom: 5,
    border: 'none',
    borderBottom: '0.5px solid lightgray',
    background: 'transparent'
}

const screen3Container = {
    height: '100vh',
    backgroundColor: '#CED2DC',
    paddingTop: '20%'
}

const screen3Card = {
    padding: 10,
    borderRadius: 10,
    margin: '0px 5%',
    maxWidth: 675,
    backgroundColor: 'white'
}

const matrixContainer = {
    width: '100%',
    backgroundColor: '#84A4FC',
    padding: 15
}

const matrixRow = {
    borderRadius: 20,
    opacity: 1,
    display: 'flex',
    margin: 0,
}

const matrixPoint = {
    cursor: 'pointer',
    background: '#FFFFFF 0% 0% no-repeat padding-box',
    boxShadow: '0px 3px 6px #00000029',
    border: '1px solid #4B7BFF26',
    borderWidth: 1,
    display: 'flex',
    borderRadius: '50%',
    alignItems: 'center',
    justifyContent: 'center',
    margin: '2px 6px 2px 6px',
    height: '40px',
    width: '40px'
}

const winCircle = { border: '4px solid #FFFF00', borderRadius: '50%', height: '40px', }

const userCircle = { borderRadius: '50%', height: '30px', padding: 3, width: '30px', bottom: '165px' }

const userac = { border: '5px solid #37AC5D', borderRadius: '50%', height: '30px', padding: 3, width: '30px', bottom: '165px' }

const userbc = { border: '5px solid #F8D146', borderRadius: '50%', height: '30px', padding: 3, width: '30px', bottom: '165px' }

const screen3Title1 = { padding: 2, textAlign: 'center', color: 'black', fontSize: 17, fontWeight: 'bold' }

const screen3Title2 = { fontSize: 12, margin: 0, color: '#505351' }

const screen3Title3 = { textAlign: 'center', fontSize: 20, fontWeight: 'bold', color: '#FF6600' }

const screen3Title4 = { fontSize: 12, margin: 0, color: '#505351' }

const screen3Title5 = { textAlign: 'center', fontSize: 20, fontWeight: 'bold', color: '#FF6600' }

const screen3Title6 = { fontSize: 12, margin: 0, color: '#505351' }

const screen3Title7 = { textAlign: 'center', color: '#505351', fontSize: 14 }

const screen3Button1 = { marginBottom: 10, backgroundColor: '#4b7bff', color: 'white' }

const screen3Button2 = { marginBottom: 20, backgroundColor: '#4b7bff', color: 'white' }

const screen3Button3 = { marginTop: 10, marginBottom: 20, backgroundColor: '#4B7BFF', color: 'white' }

const screen3Button4 = { marginTop: 10, marginBottom: 20, backgroundColor: '#9bb2f1', color: 'white' }

const screen3Button5 = { backgroundColor: '#FFFFFF', color: 'red' }


const playerContainer = { margin: 10, height: 70, marginBottom: 15 }

const playerCard = { display: 'flex', flexDirection: 'row', width: '100%', height: '100%' }

const playerImageContainer = { width: '35%', display: 'flex', alignItems: 'center', justifyContent: 'center' }

const playerImageBorder = { borderRadius: '50%' }

const playerImage = { borderRadius: '50%', padding: '5px 8px 5px 8px', height: '40px', width: '40px', bottom: '165px' }

const playerBodyContainer = { width: '65%', display: 'flex', flexDirection: 'column', paddingRight: 10 }

const playerBody = { display: 'flex', flexDirection: 'row', height: '100%', width: '100%', alignItems: 'flex-end' }

const playerDetailWidth = { width: '50%' }

const playerDetailCard = { display: 'flex', flexDirection: 'column', height: '100%', width: '100%' }

const playerCardHeight1={ height: '40%' }

const playerCardHeight2={ height: '50%' }

const playerTitle={ color: 'grey', marginBottom: 0, fontSize: 10 }

const playerName={ color: '#424242', marginTop: 0, fontSize: 20 }

module.exports = {
    titleContainer, title, primaryTitle, secondaryTitle, button, appbar, appbarTitleContainer, appbarTitle, screen1Action,
    appbarIcon, screenOneButtonIcon, playButtonContainer, imageContainer, playButtonPrimary, playButtonSecondary, iconColor,
    screen1Card, screen1Container, actionContainer, closeButton, okButton, gameTitle, gameBody, gameRadio, gameDivider,
    screen2Container, screen2Card, screen2Divider, screen2Button,
    userContainer, userImageContainer, userImage, userTextContainer, userInput, userInputTitle,
    screen3Container, screen3Card, matrixContainer, matrixRow, matrixPoint, winCircle, userCircle, userac, userbc,
    screen3Title1, screen3Title2, screen3Title3, screen3Title4, screen3Title5, screen3Title6, screen3Title7,
    screen3Button1, screen3Button2, screen3Button3, screen3Button4, screen3Button5,
    playerContainer, playerCard, playerImageContainer, playerImageBorder, playerImage, playerBodyContainer, playerBody,
    playerDetailWidth, playerDetailCard,playerCardHeight1,playerCardHeight2,playerTitle,playerName
};
